<nav class="navbar navbar-expand-lg navbar-dark bg-danger">
  <i class="fab fa-accessible-icon" style="color:white"></i> <%= link_to "Cinema", product_index_path, class: "navbar-brand "%>
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navb" aria-expanded="true">
    <span class="navbar-toggler-icon"></span>
  </button>
  <ul class="nav navbar-nav ml-auto">
    <% if current_user %>
      <li class="nav-item">
        <a class="nav-link" href="#" style="color:white"><span class="fas fa-user"></span><%= current_user.email %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><%= link_to "#{ t('products.index.logout')}", log_out_path, method: :delete %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><span class="fas fa-sign-in-alt"></span><%= link_to "#{ t('products.index.admin')}", admin_products_path %></a>
      </li>
    <% else %>
      <li class="nav-item">
        <a class="nav-link" style="color:white"><span class="fas fa-user"></span><%= link_to "#{ t('products.index.create')}", sign_up_path %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><span class="fas fa-sign-in-alt"></span><%= link_to "#{ t('products.index.login')}", log_in_path %></a>
      </li>

    <% end %>
  </ul>
</nav>